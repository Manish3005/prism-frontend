# FastAPI gateway base URL (Render / local)
VITE_API_BASE_URL=http://localhost:8000

# Polling interval (ms) for async job status after 202 Accepted
VITE_STATUS_POLL_INTERVAL=2000

# Enable mock mode when backend is offline (true/false)
VITE_USE_MOCK_API=false
